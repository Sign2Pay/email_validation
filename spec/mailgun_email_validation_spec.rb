require 'spec_helper'

describe MailgunEmailValidation do
  it 'has a version number' do
    expect(MailgunEmailValidation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
